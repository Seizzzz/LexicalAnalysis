void func()
{
	return -1 - 180.3e-2 * 1e-1 / 2.0E-2 + 200%2;
}

int main()
{
	
	int a = 4.5, b = 4.4.6; /*error*/
	if(a==b && 2|3 || 1&2) a = a+b; //test
	else a = a > b + a < b;
	a += b;
	b /= a;
	if(a>=2 && a<=3 || b>=3 && b<=4) return sizeof(double);
	printf("%d\"123\3%d", a);
	return 0;
} 